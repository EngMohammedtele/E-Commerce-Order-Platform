package com.codelegends.ecommerce.repository;
// Marker class; repository interfaces are kept in separate files for Spring Data discovery.
// Defines the Repositories type used by the ecommerce app
public final class Repositories {
    private Repositories() {
    }
}
